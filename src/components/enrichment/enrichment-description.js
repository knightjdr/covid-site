import React from 'react';

const EnrichmentDescription = () => (
  <p>
    Search by GO term, GO ID or keyword for viral proteins whose proximity interactions
    are enriched for those terms.
  </p>
);

export default EnrichmentDescription;
